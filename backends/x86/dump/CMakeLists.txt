info("${VT100_BOLD}${VT100_FG_CYAN}NABLA${VT100_RESET} adding ${VT100_FG_YELLOW}LIBRARY${VT100_RESET}: ${VT100_BOLD}${VT100_FG_YELLOW}DUMP${VT100_RESET}")

set(BACKEND_LIB_DUMP_C dump.c
  ${CMAKE_CURRENT_BINARY_DIR}/dump.S)

set_property(SOURCE
  ${CMAKE_CURRENT_BINARY_DIR}/dump.S 
  PROPERTY OBJECT_DEPENDS 
  ${CMAKE_CURRENT_SOURCE_DIR}/*.*)

configure_file(dump.S ${CMAKE_CURRENT_BINARY_DIR}/dump.S)

add_library(NablaX86Dump ${BACKEND_LIB_DUMP_C})
