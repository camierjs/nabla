set(MPI_C_COMPILER /usr/local/bin/mpicc)
set(MPI_CXX_COMPILER /usr/local/bin/mpicxx)
set(MPI_INCLUDE_PATH /usr/local/include)

set(HYPRE_ROOT /usr/local)

set(Trilinos_ROOT_PATH /usr/local/trilinos/11.12.1)

set(ICET_ROOT /usr/local)

set(RAJA_ROOT /usr/local)

set(KOKKOS_ROOT /usr/local)

set(ARCANE_ROOT /usr/local)

set(LEGION_ROOT /usr/local)

set(MFEM_ROOT_PATH /usr/local)
set(GLVIS_ROOT_PATH /usr/local)

